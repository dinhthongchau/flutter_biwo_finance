
enum TimeFilterHome { daily, weekly, monthly }

enum TimeFilterAnalysis { daily, weekly, monthly, year }

enum ReportTypeSearch { all, income, expense, saving }

enum ListTransactionFilter { daily, weekly, monthly, all }

enum MoneyType { expense, income, save }

enum ChartTypeCalendar { spends, categories }